# **Project MaJic: Macro JSON Interface for Configuration**

Version: 1.0.0 (Draft)  
Date: 2025-05-20

## **1\. 概要 (Overview)**

**MaJic (Macro JSON Interface for Configuration)** は、人間が読み書きするための「柔軟性」と、システムが解釈するための「厳密性」を両立させた、次世代の設定記述言語です。

HJSON や YAML のような可読性を持ちながら、JavaScriptベースのマクロシステムを搭載し、データの構造化とロジックの記述を同時に行うことができます。

### **1.1 開発の背景**

JSONはシステム間のデータ交換には最適ですが、人間が記述・管理するには以下の課題がありました。

* **記述の冗長性**: ダブルクォートや括弧の多用。  
* **ロジックの欠如**: 条件分岐や繰り返し処理ができず、コピペが増える。  
* **コメント不可**: 設定の意図を残せない。

一方で、YAMLやHJSONは「曖昧さ」を許容しすぎる傾向があり（ノルウェー問題やインデントミス）、大規模な設定ファイルでは脆弱性が露呈します。

MaJic はこれらの課題に対し、**「Strict Comma（厳密な区切り）」** と **「Macro System（構造化ロジック）」** というアプローチで解決策を提示します。

## **2\. 言語仕様 (Language Specification)**

MaJic のファイル拡張子は .mj または .majic を使用します。

### **2.1 基本構文 (Syntax Rules)**

1. **Strict Comma (厳密な区切り)**  
   * 配列やオブジェクトの要素区切りには**カンマ (,) が必須**です。  
   * これにより、改行コードが単なる空白として扱われ、意図しない行結合（HJSONの弱点）を防ぎます。  
2. **Quoteless Strings (クォートレス文字列)**  
   * 構造を表す予約文字（@, ", ', \[, {, \], }, ,）で始まらない限り、文字列のクォートを省略できます。  
   * **日本語（マルチバイト文字）もそのまま記述可能**です。  
   * 値の終端は「カンマ」または「改行」で判定されます。  
3. **Comments**  
   * \#, // (行コメント)  
   * /\* ... \*/ (ブロックコメント)

### **2.2 マクロ (Macros)**

@ で始まる識別子はマクロ呼び出しとして扱われます。

* **構文**: @MacroName(arg1, arg2, ...)  
* 引数には MaJic のあらゆる値（ネストしたマクロ、オブジェクト、配列）を渡せます。

### **2.3 タグ付きリテラル (Tagged Literals)**

埋め込み言語をサポートするために、識別子をプレフィックスにした文字列定義が可能です。  
md'''...''' や js'''...''' のように記述します。

* **用途**: エディタ上でのシンタックスハイライト、バリデーション、専用のパース処理。  
* **出力**: JSON変換時にはタグ情報は削除され、純粋な文字列として出力されます。

### **2.4 Frontmatter (Scripting Context)**

ファイルの先頭に \--- で区切られたブロックを記述することで、JavaScript のコンテキストを定義できます。外部ライブラリの読み込みや定数定義に使用します。

\---  
const IS\_PROD \= process.env.NODE\_ENV \=== 'production';  
const { Say } \= require('./rpg\_macros');  
\---  
{  
  debug\_mode: @Var(IS\_PROD)  
}

## **3\. コア機能 (Core Features)**

### **3.1 Undefined Erasure (条件分岐と消滅)**

MaJic には if 文のような制御構文はありません。代わりに以下のルールを採用しています。

**「マクロが undefined を返した場合、その要素は親構造から削除（消滅）される」**

* **配列内**: その要素が削除され、後ろの要素が詰められます。  
* **オブジェクト内**: そのキー自体が削除されます。

これにより、@If(cond, val) マクロ一つで、宣言的な条件分岐が可能になります。

### **3.2 Smart Dedent (インデント自動除去)**

''' で囲まれた複数行文字列（Multiline String）は、**最も浅いインデントに合わせて、各行の先頭の空白が自動的に削除**されます。これにより、深いネストの中でもコードの見た目を崩さずに長文を記述できます。

### **3.3 Pure Scope & Data Integrity**

* 変数への再代入は禁止（マクロによる状態管理の禁止）。  
* @Let(scope, body) による局所的なスコープ定義のみを許可。  
  これにより、設定ファイルが「副作用の塊」になることを防ぎ、常に予測可能なJSONを出力します。

## **4\. アーキテクチャ (Architecture)**

MaJic の変換プロセスは以下の通りです。

1. **Preprocessing**: Frontmatter を Node.js VM で実行し、コンテキスト（マクロ関数や定数）を生成。  
2. **Parsing**: PEG.js (Peggy) で定義された文法に基づき、テキストを AST (抽象構文木) に変換。  
   * TaggedString などのメタ情報はここで保持されます。  
3. **Transformation**: AST をトラバースし、マクロを実行（評価）する。  
   * 再帰的なマクロ実行。  
   * undefined ノードの削除処理。  
   * Smart Dedent の適用。  
4. **Generation**: 純粋な JSON オブジェクトを出力。

## **5\. 主なユースケース (Use Cases)**

### **5.1 ゲームデータ開発 (RPGツクール等)**

* **課題**: イベントコマンドの生JSON編集は困難。日本語テキストの入力が面倒。  
* **解決**: 日本語クォートレス入力とマクロによるコマンド隠蔽。  
* **例**: @Say(勇者よ、よくぞ参った！)

### **5.2 Infrastructure as Code (Terraform, Kubernetes)**

* **課題**: 環境ごとの差異管理で設定ファイルが肥大化する。  
* **解決**: 共通リソースのコンポーネント化（マクロ化）。JSのロジックを使ったリソースの大量生成（@Map）。

### **5.3 CI/CD & Polyglot Config**

* **課題**: YAML内にシェルスクリプトやSQLを埋め込む際の記法が読みづらい。  
* **解決**: sh'''...''' や sql'''...''' によるハイライト付き記述。

## **6\. エコシステム**

* **CLI Tool**: .mj から .json へのトランスパイラ。  
* **VS Code Extension**: シンタックスハイライト、埋め込み言語サポート、マクロの入力補完。
